# Base image (match prod Airflow version)
AIRFLOW_IMAGE=apache/airflow:2.8.0

# Control plane connectivity (same as prod)
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://user:pass@prod-db:5432/airflow
AIRFLOW__CELERY__BROKER_URL=redis://prod-redis:6379/0
AIRFLOW__CELERY__RESULT_BACKEND=db+postgresql://user:pass@prod-db:5432/airflow
AIRFLOW__CORE__FERNET_KEY=replace-with-prod-fernet-key

# Worker behavior
WORKER_QUEUES=massive_trades
WORKER_CONCURRENCY=128
WORKER_HOSTNAME=kg5090@%h
# WORKER_AUTOSCALE=32,8

# DAG/runtime secrets (as needed by your tasks)
# MASSIVE_API_KEY=...
